import {inject} from '@angular/core';import {CanActivateFn,Router} from '@angular/router';import {AuthService} from '../auth/auth.service';import {Role} from '../models/models';export const authGuard:CanActivateFn=()=>{const a=inject(AuthService);return a.isAuthenticated()||inject(Router).createUrlTree(['/login'])};export const roleGuard=(roles:Role[]):CanActivateFn=>()=>inject(AuthService).hasRole(...roles)||inject(Router).createUrlTree(['/forbidden']);
