import {inject,Injectable,signal,computed} from '@angular/core';import {HttpClient} from '@angular/common/http';import {Router} from '@angular/router';import {tap} from 'rxjs';import {User} from '../models/models';
const API='http://localhost:3000/api';@Injectable({providedIn:'root'}) export class AuthService{private http=inject(HttpClient);private router=inject(Router);user=signal<User|null>(this.read<User>('tf_user'));accessToken=signal(localStorage.getItem('tf_access'));isAuthenticated=computed(()=>!!this.user()&&!!this.accessToken());login(email:string,password:string){return this.http.post<any>(`${API}/auth/login`,{email,password}).pipe(tap(r=>{localStorage.setItem('tf_access',r.accessToken);localStorage.setItem('tf_refresh',r.refreshToken);localStorage.setItem('tf_user',JSON.stringify(r.user));this.accessToken.set(r.accessToken);this.user.set(r.user)}))}register(name:string,email:string,password:string){return this.http.post<any>(`${API}/auth/register`,{name,email,password}).pipe(tap(r=>{localStorage.setItem('tf_access',r.accessToken);localStorage.setItem('tf_refresh',r.refreshToken);localStorage.setItem('tf_user',JSON.stringify(r.user));this.accessToken.set(r.accessToken);this.user.set(r.user)}))}logout(){localStorage.removeItem('tf_access');localStorage.removeItem('tf_refresh');localStorage.removeItem('tf_user');this.user.set(null);this.accessToken.set(null);this.router.navigateByUrl('/login')}hasRole(...r:string[]){return !!this.user()&&r.includes(this.user()!.role)}private read<T>(k:string):T|null{try{return JSON.parse(localStorage.getItem(k)??'null')}catch{return null}}}
